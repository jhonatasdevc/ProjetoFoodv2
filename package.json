{
  "name": "delivery-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web-loja,web-cliente,web-admin -c blue,green,red,yellow \"npm:dev:api\" \"npm:dev:web-loja\" \"npm:dev:web-cliente\" \"npm:dev:web-admin\"",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web-loja": "npm run dev --workspace=apps/web-loja",
    "dev:web-cliente": "npm run dev --workspace=apps/web-cliente",
    "dev:web-admin": "npm run dev --workspace=apps/web-admin",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
